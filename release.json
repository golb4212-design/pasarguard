{
  "version": "2.9.5",
  "architecture": "fully-split-three-worker",
  "files": {
    "core": "core/worker.js",
    "edge": "edge/worker.js",
    "processor": "processor/worker.js"
  },
  "roles": {
    "core": "central bot, finance, licenses, central APIs and D1 authority",
    "edge": "all web pages, reseller bots, customer miniapps and reseller management",
    "processor": "scheduled reseller automation, retry queue and background jobs; Telegram webhooks are processed directly by Edge"
  },
  "bindings": {
    "core": [
      "PASARGUARD_DB",
      "EDGE_WORKER",
      "PROCESSOR_WORKER"
    ],
    "edge": [
      "EDGE_DB",
      "CORE_WORKER",
      "PROCESSOR_WORKER"
    ],
    "processor": [
      "PROCESSOR_DB",
      "CORE_WORKER",
      "EDGE_WORKER"
    ]
  },
  "remote_d1": "service-binding-only",
  "request_update_checks": false,
  "health_check_mode": "cycle-safe-local-endpoints",
  "health_endpoints": {
    "core": "/__bluepanel/service/core-local-health",
    "edge": "/__bluepanel/service/edge-local-health",
    "processor": "/__bluepanel/service/processor-local-health"
  },
  "fixes": [
    "detect expired or changed representative panel credentials without exposing raw English PasarGuard errors",
    "clear only the invalid stored panel password and request a replacement from the representative owner",
    "resume the exact failed order automatically after the replacement password is verified",
    "allow verified panel passwords to replace previously stored credentials",
    "release channel announcement contains only version 2.9.5 changes"
  ],
  "credential_recovery": {
    "error_code": "AGENCY_CREDENTIALS_INVALID",
    "invalid_password_is_cleared": true,
    "telegram_password_message_deleted": true,
    "failed_order_resumed_automatically": true
  }
}
