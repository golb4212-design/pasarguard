{
  "version": "2.9.3",
  "architecture": "fully-split-three-worker",
  "files": {
    "core": "core/worker.js",
    "edge": "edge/worker.js",
    "processor": "processor/worker.js"
  },
  "roles": {
    "core": "central bot, finance, licenses, central APIs and D1 authority",
    "edge": "all web pages, reseller bots, customer miniapps and reseller management",
    "processor": "scheduled reseller automation, retry queue and background jobs; Telegram webhooks are processed directly by Edge"
  },
  "bindings": {
    "core": [
      "PASARGUARD_DB",
      "EDGE_WORKER",
      "PROCESSOR_WORKER"
    ],
    "edge": [
      "EDGE_DB",
      "CORE_WORKER",
      "PROCESSOR_WORKER"
    ],
    "processor": [
      "PROCESSOR_DB",
      "CORE_WORKER",
      "EDGE_WORKER"
    ]
  },
  "remote_d1": "service-binding-only",
  "request_update_checks": false,
  "health_check_mode": "cycle-safe-local-endpoints",
  "health_endpoints": {
    "core": "/__bluepanel/service/core-local-health",
    "edge": "/__bluepanel/service/edge-local-health",
    "processor": "/__bluepanel/service/processor-local-health"
  },
  "fixes": [
    "process reseller Telegram webhooks directly on Edge so bots no longer depend on Processor cron",
    "keep Processor for scheduled automation and background work only",
    "prevent duplicate release announcements for the same version even on repeated manual publish requests",
    "preserve release deduplication state when the update channel settings are edited",
    "refresh browser client cache markers to version 2.9.3"
  ]
}
